type Query {
    itemById(id: ID): Item
    itemByItemProperty(id: ID, name: String): Item
}

type Item {
    id: ID
    name: String
    type: Type
    itemProperty: ItemProperty
}

enum Type {
    ALPHA
    BETA
    OMEGA
}

type ItemProperty {
    id: Int
    name: String
}